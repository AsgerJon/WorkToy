# core

Most primitive runtime types: `Object`, `MetaType`,
`ContextInstance`/`ContextOwner`, and the `sentinels` (notably
`THIS`, used by `@overload(THIS)` to refer to the enclosing class).

::: worktoy.core
