# desc

Descriptor protocol: `BaseDescriptor`, `Field` (declarative
get/set with `@field.GET` / `@field.SET`), `View` (read-only Field),
`AttriBox` (runtime-typed attribute), `FixBox`, `Alias`,
`SymbolicName`.

::: worktoy.desc
