# utilities

Leaf-level utilities used across `worktoy`. Text formatting,
iterable handling, slicing, type/MRO helpers, descriptor support,
exception inspection, and the `combinatorics` subpackage.

::: worktoy.utilities
