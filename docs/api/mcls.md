# mcls

The metaclass framework. `AbstractMetaclass`, `AbstractNamespace`,
`BaseMeta`, `BaseSpace`, and `BaseObject` (the common base class
users inherit from to get overload + AttriBox support).

::: worktoy.mcls
